module.exports = function(Build) {
	/**
	 * @class build.mvc.model.Model
	 */
	Build('build.mvc.model.Model', [], function(define, $super) {
		define({
			/**
			 * @constructor
			 */
			$constructor : function Model() {
			},
		});
	});
};