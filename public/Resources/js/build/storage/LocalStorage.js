/**
 * @class build.storage.LocalStorage
 */
Build('build.storage.LocalStorage', [], function(define, $super) {
	define({
		/**
		 * @constructor
		 */
		$constructor : function LocalStorage() {

		}
	});
});